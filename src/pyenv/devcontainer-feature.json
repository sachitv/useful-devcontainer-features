{
    "name": "PyEnv",
    "id": "pyenv",
    "version": "0.0.1",
    "description": "A feature that installs pyenv and some specified python versions",
    "options": {
        "default": {
            "type": "string",
            "default": "3.11",
            "description": "The default version of Python to install"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils:2.0.10"
    ]
}
